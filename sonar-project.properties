# SonarQube Cloud project configuration.
# Pull-request parameters (sonar.pullrequest.*) are auto-detected from the
# GitHub Actions environment, so they must NOT be set here.

sonar.projectKey=wenhan-sqc-org_Gitar-Sonar-edge-case-demo
sonar.organization=wenhan-sqc-org
sonar.projectName=Gitar-Sonar-edge-case-demo

sonar.sources=orderstats
sonar.tests=tests
sonar.sourceEncoding=UTF-8

sonar.python.version=3.11
sonar.python.coverage.reportPaths=coverage.xml
